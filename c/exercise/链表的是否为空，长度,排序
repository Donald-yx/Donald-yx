#include<stdio.h>
int main(){










}
typedef struct donald{
    int data;
    struct donald*Pnext;
}NODE,*PNODE;
bool shifouweikong(PNODE pair){
    if(NULL==pair->Pnext)
    return true;
    return false;

}
bool changdu(PNODE Phead){
    PNODE p=Phead->Pnext;
    int i=0;
    while(p->Pnext!=NULL)
    {p->Pnext;i++;}



}
int paixu(PNODE Phead)
{
   int t;
    for( PNODE p=Phead->Pnext;/*初始化p相当于int i=0*/p->Pnext!=NULL;/*判断下一个节点是否为空*/p=p->Pnext/*相当于i++*/){
            for(PNODE q=p->Pnext/*相当于j=i+1*/;q->Pnext!=NULL/*判断当前节点的指针域是否为空*/;q=q->Pnext/*相当于j++*/){
                if(p->data>q->data){
                    t=p->data;
                    p->data=q->data;
                    q->data=t;
                }

            }


    }
return Phead;









}