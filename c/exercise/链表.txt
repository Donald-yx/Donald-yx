首先定义一个结构类型
数据域；
指针域；
创造链表
创建一个空的指针并且指向一个动态内存   头节点
然后判断这个指针此时是否为空{如果为空则表示内存满，终止程序，如果不空继续}
创建一个指针2也指向头节点，并且将头节点的指针域设为空（加入用户输入0个，则该节点为尾节点）


创造   让用户输入创造的节点个数
循环  
输入每一个节点的值
将该值赋值给新的节点
让新节点的地址赋值给上一个节点的指针域
将新节点的指针域为空（充当尾节点）
将新节点的地址传给指针2（让指针2 由指向上一个节点转变成指向下一个节点）
返回头指针