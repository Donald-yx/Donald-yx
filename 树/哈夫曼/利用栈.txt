在操作中加入一个栈，在定变为0还是1的时候，应该是入栈操作
在执行完该位置的回溯操作后应该进行一个出栈的操作
设置一个栈不为空的循环条件，然后在一个字符数组当中，进行操作
将栈顶元素放入字符数组，然后进行出栈弹出该元素的操作，循环将栈中元素全部出战即为该位置的哈夫曼编码哈夫曼编码

最后，这个生成的字符数组可以直接赋值给hc{i}因为hc是一个指针数组，直接将生成的字符数组名字（地址）给hc（i）就相当于
hc（i）这个位置存放了一个指针指向这个位置对应的字符数组